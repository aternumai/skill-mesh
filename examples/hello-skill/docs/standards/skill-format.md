# Skill Format Standard

This placeholder document represents the package-local authoring standard used during validation.

## Rules

- `SKILL.md` is required at the package root.
- Runtime overlays may override or extend the base instructions.
- Validation sources should be explicit and version-aware.
