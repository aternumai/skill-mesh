{
  "name": "skill-mesh",
  "version": "0.1.0",
  "private": true,
  "description": "Portable skill workspace and compatibility tooling.",
  "type": "module",
  "bin": {
    "skillmesh": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/cli.js",
    "dev": "node --loader tsx src/cli.ts"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
